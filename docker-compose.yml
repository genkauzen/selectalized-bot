services:
  selectalized-bot:
    build: .
    container_name: selectalized-bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    environment:
      DATA_DIR: /app/data
